server:
  port: 8085
  servlet:
    context-path: /jasper

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl: auto-update
        show-sql: true
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/db
    username: username
    password:

